repos:
  - repo: local
    hooks:
      - id: localeo-guidance
        name: Liens des guides Localeo dans l'index Git
        entry: python scripts/check_guidance.py --repo . --staged
        language: system
        pass_filenames: false
        always_run: true
